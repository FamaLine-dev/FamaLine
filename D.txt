# Last resort: find any executable named init
  local found
  found=$(find /.root/sbin /.root/usr/sbin /.root/bin /.root/usr/bin \
          -maxdepth 1 -name 'init' -executable 2>/dev/null | head -1)
  if [ -n "$found" ]; then
    echo "${found#/.root}"
    return 0
  fi
  return 1
}
